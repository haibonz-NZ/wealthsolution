# 报告生成系统：技术方案回顾与战略调整建议

**日期**：2026-01-11
**现状诊断**：
当前系统采用 **“方案 A (智能模块化组装)”**，即“AI 写内容 + 代码画图表”。架构本身是先进且稳定的，但核心动力源 —— **Google Gemini API** —— 在国内网络环境下表现出了极高的不稳定性（墙、Key 封禁、模型版本混乱）。

为了彻底解决“生成失败”的焦虑，我们将之前的三个方案调出回顾，并提出一个**针对中国环境的终极解决方案**。

---

## 🔄 原有方案回顾 (技术架构层)

这是我们在项目初期讨论的三种**渲染/实现方式**：

### 方案 A：智能模块化组装 (Smart Modular Assembly) —— 【当前采用】
*   **原理**：AI 只负责写文字（摘要/痛点/建议），系统负责画精确的图表（家族树/饼图），最后拼装在一起。
*   **优点**：视觉效果最专业，图表数据绝对准确，支持打印。
*   **缺点**：**严重依赖 AI API 的稳定性**。如果 AI 连不上，文字部分就开天窗。

### 方案 B：AI 原生流式 UI (Generative UI)
*   **原理**：让 AI 直接写代码来生成界面。
*   **优点**：灵活度极高。
*   **缺点**：**极其不稳定**。如果 Gemini 现在连文字都生成不出，让它写代码只会产生更多的 Bug 和白屏。此方案不适合当前网络环境。

### 方案 C：静态排版引擎 (Static PDF)
*   **原理**：后台静默生成 PDF 文件。
*   **优点**：格式固定。
*   **缺点**：无交互，且同样需要 AI 来生成内容，并未解决“AI 连不上”的核心痛点。

---

## 🚀 新战略建议：更换 AI 引擎 (Core Engine Shift)

既然“方案 A”的架构没有问题（图表和排版都很漂亮），问题出在“连接 Google”上，那么**换一个不需要梯子、稳定且强大的 AI 模型**才是治本之策。

### 🌟 方案 D：切换至 DeepSeek (深度求索) —— 【强烈推荐】

**DeepSeek-V3** 是目前中国最强的开源大模型，能力对标 GPT-4 和 Gemini Ultra，但对中国开发者极其友好。

*   **核心优势**：
    1.  **国内直连**：服务器在境内，**不需要任何代理/梯子**，本地 `localhost` 也能秒连，速度飞快。
    2.  **永不封号**：正规国产服务，不会因为 IP 变动而封禁 Key。
    3.  **极其便宜**：价格仅为 GPT-4 的 1/100，几乎等于免费。
    4.  **中文能力更强**：更懂中国家族的语境（如“代持”、“户口”、“外汇管制”）。

*   **迁移成本**：**极低**。
    *   DeepSeek 的 API 完全兼容 OpenAI 格式。
    *   我们只需修改 `ReportService.ts` 中的 API 地址和 Key，前端逻辑几乎不用动。

### 方案 E：降级为“模板填充模式” (No-AI Fallback)

*   **原理**：彻底放弃 AI 实时生成。
*   **逻辑**：系统内置几十套“预设话术”。根据用户的资产规模（>1亿 或 <1000万）和资产分布（国内/国外），自动拼接预设的段落。
*   **优点**：**100% 成功率**，绝不出错，无需联网。
*   **缺点**：**内容千篇一律**，缺乏针对性，看起来像“填空题”，不够高端。

---

## 🏆 专家委员会最终建议

**维持【方案 A (模块化组装)】的界面架构，但将 AI 引擎切换为【方案 D (DeepSeek)】。**

*   **理由**：您希望报告看起来“专业、高端、有深度”，这必须依靠 AI 的分析能力（模板法做不到）。而 DeepSeek 能在保证深度的同时，彻底解决“连接失败”和“404 报错”的技术顽疾。

### ✅ 执行路径：
如果您同意切换到 DeepSeek：
1.  我们会将系统中的 API 接口地址从 Google 改为 DeepSeek。
2.  您只需去 DeepSeek 官网申请一个 Key（我们会提供指引）。
3.  系统将彻底告别网络报错，从此稳定运行。
