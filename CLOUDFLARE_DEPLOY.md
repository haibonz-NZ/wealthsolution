# Cloudflare Pages 部署指南

恭喜！您的代码已经成功上传到 GitHub。现在，我们使用 Cloudflare Pages 将其发布到全球互联网。

Cloudflare Pages 是一个免费、极速且自动化的静态网站托管服务，非常适合本项目（React + 边缘函数）。

---

## 第一步：登录 Cloudflare

1.  访问 [Cloudflare Dashboard](https://dash.cloudflare.com) 并登录（如果没有账号请注册）。
2.  在左侧菜单中点击 **Workers & Pages**。
3.  点击蓝色的 **Create application** 按钮。
4.  切换到 **Pages** 标签页。
5.  点击 **Connect to Git** 按钮。

## 第二步：连接 GitHub 仓库

1.  Cloudflare 会请求访问您的 GitHub 账号，请授权。
2.  在仓库列表中，选择您刚才上传的 **`wealthsolution`** 仓库。
3.  点击 **Begin setup**。

## 第三步：配置构建参数 (关键步骤！)

在 "Set up builds and deployments" 页面，请按照以下标准填写：

*   **Project name**: 保持默认 (例如 wealthsolution)
*   **Production branch**: `main`
*   **Framework preset** (框架预设): 选择 **Vite**  <-- *非常重要*
*   **Build command** (构建命令): `npm run build` (选择 Vite 后会自动填入)
*   **Build output directory** (输出目录): `dist` (选择 Vite 后会自动填入)

## 第四步：部署上线

1.  点击 **Save and Deploy** 按钮。
2.  Cloudflare 会开始自动构建。
    *   它会下载代码、安装依赖 (npm install)、构建网站 (npm run build)。
    *   它还会自动识别 `functions/api/generate.js` 并部署后端服务。
3.  等待约 1-2 分钟，看到 "Success!" 提示后，点击屏幕上方的链接（例如 `https://wealthsolution.pages.dev`）。

## 第五步：设置环境变量 (安全加固与鉴权) [强烈推荐]

为了保护您的 API Key 不被泄露，并防止未经授权的用户盗用您的额度，请务必设置以下环境变量。

1.  进入您的 Cloudflare Pages 项目后台。
2.  点击顶部的 **Settings** (设置) 标签页。
3.  在左侧菜单点击 **Environment variables** (环境变量)。
4.  点击 **Add variable** (添加变量) 按钮。
5.  添加以下变量：
    *   **Variable name**: `GOOGLE_API_KEY`
    *   **Value**: `您的 Google Gemini API Key` (例如 AIzaSy...)
    *   **Variable name**: `ACCESS_CODE` (访问密码)
    *   **Value**: `设置一个复杂密码` (例如 my-secret-code-888，只有知道此密码的人才能使用 AI 功能)
    *   *(可选)* **Variable name**: `AI_MODEL`
    *   **Value**: `gemini-1.5-pro` (默认模型)
6.  点击 **Save**。

**注意**：环境变量设置后，需要**重新部署**一次才能生效。

## 🚨 紧急安全操作：清洗 Git 历史

如果您收到了 Google 的泄露警告，说明 Key 已经存在于 GitHub 的历史提交记录中。仅删除代码中的 Key 是不够的，必须清洗历史：

1.  **删除历史**：在您的本地项目文件夹中，找到并删除隐藏的 `.git` 文件夹。
2.  **重建仓库**：
    *   打开终端，输入 `git init`
    *   输入 `git add .`
    *   输入 `git commit -m "Security update"`
    *   输入 `git branch -M main`
    *   输入 `git remote add origin git@github.com:haibonz-NZ/wealthsolution.git` (替换为您的仓库地址)
    *   输入 `git push -u origin main --force`
3.  **完成**：这样 GitHub 上包含 Key 的旧记录就会被彻底覆盖消失。

## 第六步：最终配置

1.  打开您部署好的网站链接。
2.  进入 **“系统设置”** -> **“AI 模型配置”**。
3.  **连接模式**：选择 **Cloudflare Pages (推荐)**。
4.  **Google API Key**：如果已经配置了第五步的环境变量，这里可以**留空**。系统会自动使用服务器端的 Key。

---

## 进阶：绑定自定义域名 (GoDaddy)

如果您拥有自己的域名（例如 `wealthsolution.theonefo.com`），可以将其绑定到 Cloudflare Pages。

1.  进入 Cloudflare Pages 项目页面 -> **Custom domains**。
2.  点击 **Set up a custom domain**，输入 `wealthsolution.theonefo.com`。
3.  在 GoDaddy DNS 管理中添加 CNAME 记录：
    *   Type: `CNAME`
    *   Name: `wealthsolution`
    *   Value: `wealthsolution.pages.dev`
4.  回到 Cloudflare 点击 **Activate domain** 等待生效。
